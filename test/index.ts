import { RIPEClient } from '../src/client/RIPEClient';

const client = new RIPEClient({
  sourceapp: 'app1'
});

/*client
  .asOverview({
    resource: 3333,
    meta: 'methodology'
  })
  .then(res => {
    console.log(res);
  });*/
